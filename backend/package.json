{
  "name": "lexis-backend",
  "version": "2026.3.0",
  "description": "LEXIS v2026.3 — Secure WebRTC Token Broker",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node test/fair-use.test.mjs",
    "dev": "node --watch server.mjs",
    "build": "mkdir -p dist && echo '<!doctype html><title>LEXIS API</title><body>LEXIS backend — see /health' > dist/index.html"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-fetch": "^3.3.2",
    "stripe": "^16.12.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
